// Welcome to algorithms🦄















// if (num >59){
  //   let hours = Math.floor(num / 60)
  //   let minutes = num%60
  //   if (minutes <10){
  //     return `${hours}:0${minutes}`
  //   }
  //   return `${hours}:${minutes}`
  // } if (num < 10){
  //   return  `0:0${num}`
  // }
  // return `0:${num}`
  